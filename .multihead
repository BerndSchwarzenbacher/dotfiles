#!/bin/bash

IN="eDP1"
EXT="DP2"

if (xrandr | grep "$EXT disconnected"); then
  xrandr --output $EXT --off --output $IN --dpi 144 --auto
else
  xrandr --output $IN --dpi 144 --auto --output $EXT --auto --right-of $IN
fi
